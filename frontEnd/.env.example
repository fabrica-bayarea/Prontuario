# Variáveis de ambiente do Frontend (Vite)
# Copie este arquivo para .env e ajuste se necessário

# URL da API do backend (para desenvolvimento local sem Docker)
VITE_API_URL=http://localhost:3001/api
